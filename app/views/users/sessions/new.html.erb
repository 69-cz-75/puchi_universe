<body>
  <div class="relative flex flex-col justify-center min-h-screen overflow-hidden">
    <div class="p-6 sm:mx-6 md:m-auto bg-white rounded-md shadow-md ring-2 ring-gray-800/50 sm:max-w-lg">
      <h1 class="text-3xl font-semibold text-center text-gray-700"><%= t 'defaults.login' %></h1>
      <br>
      <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>

        <div class="field mb-5">
        <label for="user_email" class="font-semibold"><%= f.label :email %></label>
          <%= f.email_field :email, class: "w-full input input-bordered", autofocus: true, autocomplete: "email" %>
        </div>

        <div class="field mb-5">
        <label for="user_password" class="font-semibold"><%= f.label :password %></label>
          <%= f.password_field :password, class: "w-full input input-bordered", autocomplete: "current-password" %>
        </div>

        <% if devise_mapping.rememberable? %>
          <div class="field mb-5">
            <%= f.check_box :remember_me %>
            <%= f.label :remember_me %>
          </div>
        <% end %>

        <div class="actions mb-7">
          <%= f.submit (t 'defaults.login'), class: "btn btn-block"%>
        </div>
      <% end %>

      <hr class="mb-5">
      <div class="text-center mb-3">
        <%= link_to(t('defaults.signup'), new_user_registration_path, class: 'text-base text-gray-600') %>
      </div>
      <!-- パスワードリセットのリンクは後ほど整える -->
      <!-- div class="text-center" -->
        <!-- a href="<%= new_user_password_path %>" class="text-base text-gray-600">Forgot your password?</a -->
      <!-- /div -->
    </div>
  </div>
</body>